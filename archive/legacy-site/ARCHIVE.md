# Legacy Onsano website

This folder preserves the static website that previously occupied the
`davidalesch/onsano_web` repository before it was replaced by the current Astro
website.

The original files are `index.html` and `README.md`. Open `index.html` directly
to view the archived version.
